services:
  backend:
    build: .
    ports:
      - "8123:8123"
    env_file:
      - .env
    volumes:
      - ./app:/app/app

  frontend:
    build: ./frontend
    ports:
      - "3200:3200"
    volumes:
      - ./frontend/src:/app/src
      - /app/node_modules
    depends_on:
      - backend
